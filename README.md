# Image-
A responsive image gallery that displays images in a 3-column grid on desktop and adjusts to a single column on mobile devices. It uses CSS Grid for layout and ensures images resize neatly with smooth spacing.
